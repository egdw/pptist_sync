# PPTist 大屏部署包（RK3588 / Linux aarch64）

把做好的 PPT 变成「通电即放映」的大屏：板子上解压本包 → 执行一次 `./setup.sh` → 以后桌面双击「PPTist 大屏播放」即可。

## 包内容

| 文件/目录 | 说明 |
| --- | --- |
| `dist/` | 已构建好的 PPTist 前端页面（无需再编译） |
| `server/pptist-server.mjs` | PPTist 服务端（页面 + 默认 PPT 管理 + 更新通知） |
| `server/showflow-ws.mjs` | 多屏联动 WebSocket 服务（联动编排/副屏同步） |
| `server/node_modules/ws` | WebSocket 依赖（已随包捆绑，无需联网安装） |
| `reveal-example/` | 副屏 Reveal / Markdown 演示页（服务端在 /reveal 自动托管） |
| `runtime/node-v20.18.1-linux-arm64.tar.xz` | 自带的 Node.js 运行时（首次部署自动解压，无需联网） |
| `setup.sh` | 一键部署脚本（解压运行时、生成配置和桌面快捷方式） |
| `start-pptist.sh` | 启动服务并打开浏览器（可带参数 `play` / `editor` / `upload` / `showflow` / `secondary` / `reveal`） |
| `stop-pptist.sh` | 停止服务 |
| `enable-boot-service.sh` | 注册 systemd 服务、立即启动并设置真正的开机自启 |
| `disable-boot-service.sh` | 取消 systemd 开机自启（不会删除文稿和照片） |
| `logo.png` | 桌面快捷方式图标 |

## 首次部署（三步）

1. 把整个 `pptist` 目录拷贝到 RK3588 板子（U 盘 / scp 均可），建议放到家目录，例如 `/home/user/pptist`；
2. 在该目录打开终端执行 `bash setup.sh`（如需开机自动放映则执行 `bash setup.sh --autostart`；
   提示权限不足时先执行 `chmod +x *.sh`）；
3. 桌面双击 **「PPTist 大屏播放」** —— 服务自动启动，浏览器自动以全屏（kiosk）方式打开播放页。

> 需要 `xz` 命令解压自带运行时（多数系统自带；缺失时脚本会提示 `sudo apt install xz-utils`）。
> 若桌面环境没有 `~/Desktop` 目录（如中文系统为 `~/桌面`），脚本会自动识别。

## 已有部署升级（数据不丢）

所有数据都在安装目录的 `data/` 里（主屏/副屏文稿、Studio 草稿与版本、素材、
主题、联动方案、MQTT 配置），`config.env` 在安装目录根。升级只覆盖程序文件，
**不触碰 data/ 与 config.env**：

1. 备份（稳妥起见）：`cp -r /home/user/pptist/data /home/user/pptist-data-backup`；
2. 把新包里的 `dist/`、`server/`、`reveal-example/`、`setup.sh`、
   `start-pptist.sh`、`stop-pptist.sh` 覆盖到板子旧目录
   （直接 `tar -xzf pptist-rk3588-deploy.tar.gz -C /home/user/` 解压覆盖即可，
   tar 只覆盖同名文件、不删除 data/ 等其他内容）；
3. `./stop-pptist.sh && ./start-pptist.sh` —— 完成后主屏/副屏文稿、
   Studio 草稿、联动方案、MQTT 配置全部原样保留。

注意两点：
- `reveal-example/theme.css` 是 LCD/Reveal「默认主题」的载体，覆盖后会回到
  包内版本；自定义主题存在 `data/studio/themes/`，不受影响。
- ShowFlow 编排方案同时存在浏览器 localStorage 与服务端
  `data/showflow/state.json`，升级后浏览器普通刷新一次即自动同步。

## 日常使用

| 操作 | 方式 |
| --- | --- |
| 大屏放映 | 双击桌面「PPTist 大屏播放」（服务没启动会自动拉起） |
| 更换 PPT | 局域网任意电脑浏览器打开 `http://<板子IP>:8686/upload`，上传 .pptx 并点「上传并设为默认」，播放页 1 秒内自动切换 |
| 编辑文稿 | 双击应用菜单「PPTist 编辑器」或访问 `http://<板子IP>:8686/editor` |
| 停止服务 | 执行 `./stop-pptist.sh`（直接关机也无需额外操作） |

默认页面地址（端口可在 `config.env` 修改）：

- 播放页：`http://<板子IP>:8686/play`
- 上传页：`http://<板子IP>:8686/upload`
- 编辑器：`http://<板子IP>:8686/editor`
- 联动编排：`http://<板子IP>:8686/showflow`
- 副屏 PPTist：`http://<板子IP>:8686/secondary`
- 副屏 Reveal：`http://<板子IP>:8686/reveal`

## 多屏联动编排（Virtual Show Flow）

独立于 PPT 页码的"虚拟播放序列"：提前把主屏页面、副屏页面按演出顺序拖拽编排，
放映时只按 →/← 逐个执行虚拟步骤，主副屏各自跳转到正确页面（页数无需一致）。

**典型使用流程：**

1. 上传两份 PPT：打开 `/upload`，上传目标选「主屏文稿」传主屏 PPT；
   再把目标切到「副屏文稿（PPTist B）」传副屏 PPT（两份完全独立，页数可不同）。
2. 主控电脑打开 `/showflow` 编排页：左侧主屏页面池、右侧副屏页面池（带缩略图），
   点击或拖拽页面到中间序列，支持仅主屏 / 仅副屏 / 主副同步步骤；
   编排好的方案可「另存为」多份随时切换。
3. 副屏电脑打开 `/secondary`（PPTist B）或 `/reveal`（Markdown 流程图），
   页面会自动注册为副屏并等待控制。
4. 编排页打开「启用多屏联动」→ 点「开始联动放映」：主屏全屏放映，
   右下角迷你控制条显示当前步骤，按 →/← 逐步骤推进；副屏自动跟随并确认（ACK）。
5. 异常兜底：悬停迷你条可展开「重发 / 同步 / 强完 / 跳副 / 退出」等手动操作；
   若提示「控制台在其他窗口」，点「接管控制台」即可夺回控制权。

**注意：** 页面引用基于稳定 pageId（PPTist slideId / Reveal data-page-id），
源 PPT 增删页、调序后编排关系不乱；重新上传同槽位文稿后，在编排页点
「刷新副屏清单」会自动对账并提示变更。

## 配置（config.env）

首次执行 `setup.sh` 时自动生成，可手动编辑后重启服务：

```
PPTIST_PORT=8686                          # 服务端口
PPTIST_PUBLIC_URL=http://192.168.x.x:8686 # 对外访问地址（自动填入探测到的局域网 IP）
PPTIST_DATA_DIR=/home/user/pptist/data    # 主屏文稿保存位置
PPTIST_SECONDARY_DATA_DIR=.../data/secondary-ppt # 副屏文稿（PPTist B）保存位置，与主屏独立
# PPTIST_MAX_UPLOAD_MB=1024                 # 上传大小上限，默认 1GB，需要调整时取消注释
```

## 开机即放映

执行 `./setup.sh --autostart`，之后板子通电登录桌面时会自动启动服务并全屏打开播放页。
要求完全无人值守（不登录桌面直接全屏）时，可把浏览器 kiosk 命令注册为系统服务：
`chromium-browser --kiosk http://127.0.0.1:8686/play`。

## 后台服务开机自启（推荐）

在部署目录执行一次：

```bash
bash enable-boot-service.sh
```

脚本会请求 sudo 权限，自动创建 `/etc/systemd/system/pptist.service`，注册当前用户，
执行 `systemctl enable --now`。此方式不依赖用户登录桌面，机器启动后服务端即可供局域网访问。

常用管理命令：

```bash
sudo systemctl status pptist.service
journalctl -u pptist.service -f
sudo systemctl restart pptist.service
bash disable-boot-service.sh
```

systemd 服务只启动后端，不主动打开浏览器。需要开机后自动打开全屏浏览器时，另外执行
`bash setup.sh --autostart` 注册桌面登录后的 kiosk 入口。

## ShowFlow Studio

浏览器访问 `http://设备IP:8686/studio`。页面草稿、正式版本、发布历史和上传素材统一保存在
`data/studio/`；编辑只更新 Draft，点击“发布”后 `/reveal/slides.md` 才切换到新的 Active 内容。
升级时保留整个 `data/`，备份或迁移 Studio 时单独复制 `data/studio/` 即可。
Reveal Theme、LCD Theme、CSS Draft、发布历史和素材都包含在该目录中。

## 常见问题

- **提示缺少 xz / tar**：`sudo apt install -y xz-utils` 后重新执行 `./setup.sh`。
- **双击桌面图标没反应**：先在终端执行 `./start-pptist.sh` 查看输出；确认当前用户在桌面会话中。
- **升级后页面仍显示旧版 / 100MB 上限 / 提示服务端版本较旧**：说明旧服务进程还占着端口。执行
  `pkill -f pptist-server.mjs; sleep 1; ./start-pptist.sh`，看到
  「服务就绪（新协议已生效，上传上限 1024MB）」即彻底解决（新版启动脚本每次启动会自动做这一步）。
- **其他电脑打不开上传页**：检查板子防火墙（`sudo ufw allow 8686` 或临时关闭），并确认与板子在同一网段；`config.env` 中 `PPTIST_PUBLIC_URL` 需为板子的实际局域网 IP。
- **浏览器不是全屏**：脚本已用 `--kiosk` 打开 Chromium 系浏览器；若使用 Firefox 则为 `--kiosk` 窗口全屏。也可用页面内右上角「全屏」按钮。
- **默认 PPT 存在哪里**：`pptist/data/default-ppt/versions/v<序号>/`（原始文件 + 解析数据 + 元数据），升级/重启不丢失；备份该目录即备份全部数据。
- **升级新版本**：用新版包整体覆盖（保留自己的 `data/` 目录与 `config.env`），然后执行 `./start-pptist.sh`（会自动清理旧进程）即可，数据不受影响。注意 `dist/`（页面）与 `server/`（服务端）必须来自同一个版本的包，两者配套升级。

## RK3588 系统侧稳定性配置（双屏协同防冻结，2026-09 实测总结）

双屏协同长时间运行出现「网页无响应 / 整机假死」的实测根因，除程序修复外
还需要以下**系统侧配置**，新板子部署时务必执行一次：

1. **禁用 Chromium 硬件视频解码**（vendor MPP 内核在多路并发解码会话下会
   `alloc_task failed` 并拖垮共享 GPU 进程，双窗口全部冻结；文稿中的大 GIF
   转码成视频后播放会走到这条路径）：

   ```bash
   sudo tee /etc/chromium-browser/customizations/10-disable-hw-video-decode <<'EOF'
   # PPTist RK3588: 禁用硬件视频解码——vendor MPP 多会话缺陷会冻结整机，软解更稳
   CHROMIUM_FLAGS="$CHROMIUM_FLAGS --disable-accelerated-video-decode"
   EOF
   sudo chmod 755 /etc/chromium-browser/customizations/10-disable-hw-video-decode
   ```

   注意：浏览器必须经 `/usr/bin/chromium-browser`（wrapper）启动才会加载该目录，
   直接调 `/usr/lib/chromium-browser/chromium-browser` 二进制不生效。

2. **加应急 swap**（板子默认无 swap，内存耗尽时回收风暴会拖死整机——连
   ping/SSH 都无响应约 15 分钟；有 swap 只会变慢不会死机）：

   ```bash
   sudo fallocate -l 6G /swapfile && sudo chmod 600 /swapfile
   sudo mkswap /swapfile && sudo swapon /swapfile
   echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
   echo 'vm.swappiness=10' | sudo tee /etc/sysctl.d/99-pptist-swap.conf
   sudo sysctl -q vm.swappiness=10
   ```

3. **双屏 kiosk 用两个独立实例启动**（单实例开两次会把副屏页面塞进隐藏标签页，
   副屏物理屏幕空白；独立实例还各自持有 GPU 进程，单屏故障互不拖累）：

   ```bash
   sudo apt-get install -y xdotool   # GNOME 按鼠标所在屏放置全屏窗口，需用指针引导
   bash start-dual-kiosk.sh          # 主屏 /play 左 + 副屏 /secondary 右
   ```

   **HDMI 分配与完全重启**（v2）：

   ```bash
   bash start-dual-kiosk.sh                            # 自动探测: HDMI-1=主屏, HDMI-2=副屏
   bash start-dual-kiosk.sh --main=HDMI-2 --sec=HDMI-1 # 指定哪块 HDMI 显示主屏/副屏
   bash start-dual-kiosk.sh restart                    # 完全重启: pptist 服务 + 双屏浏览器,
                                                       # 两屏自动恢复到重启前的画面
   bash start-dual-kiosk.sh stop                       # 只关浏览器（页面服务仍在线）
   bash start-dual-kiosk.sh status                     # 查看显示器/实例/联动运行时
   ```

   画面恢复原理：联动放映的当前虚拟步骤持久化在 `data/showflow/runtime.json`，
   播放页每次启动自动跳回该步骤（副屏经控制器同步恢复），完全重启不丢放映进度。
   HDMI 分配也可写入 `config.env`：`PPTIST_MAIN_DISPLAY=HDMI-1`、
   `PPTIST_SEC_DISPLAY=HDMI-2`。

   **现场（陌生屏幕/投影/LED）适配要点**：
   - 连接器名、分辨率、数量都无需预配置——脚本自动探测活动显示器，
     每块窗口按该屏实际分辨率全屏，页面内容自动缩放；
   - 到场接好线后先跑 `bash start-dual-kiosk.sh check` 自检（显示器数量、
     服务、文稿、禁硬解、看门狗、swap 一并核对，全部 ✓ 即可开演）；
   - 自动分配默认「第一块屏=主屏、第二块屏=副屏」；接反了用
     `--main=<名> --sec=<名>` 对调（名字看 check 输出）；
   - 现场只提供一块屏时自动降级：主屏全屏 + 副屏右半屏同屏；
     只跑主屏可用 `--sec=none`；
   - 若两屏显示相同画面（镜像模式），到系统「设置 → 显示器」改为
     「连接显示」（扩展模式）后再启动脚本。

4. **双屏看门狗（自动恢复卡死/白屏，务必安装）**。即使做了以上防护，共享服务器上
   渲染器崩溃 / GPU 合成器卡死仍可能发生（表现为某屏白屏或画面停住）。看门狗每
   5 分钟检查两屏心跳与确认状态：主屏心跳断、副屏心跳断、或"有新切换步骤但副屏
   10 分钟不回 ACK"（合成器卡死特征），连续 2 轮命中即自动重启对应屏的实例，
   **约 15 秒内恢复放映**；每日凌晨 4:30 主动重启两屏防长时运行积累。每屏动作
   限速 10 分钟，避免误动作。

   ```bash
   sudo install -m 755 pptist-secondary-watchdog.sh /usr/local/bin/
   sudo install -m 644 deploy/pptist-watchdog.service deploy/pptist-watchdog.timer /etc/systemd/system/
   sudo systemctl daemon-reload && sudo systemctl enable --now pptist-watchdog.timer
   # 查看动作日志：journalctl -t pptist-watchdog -f
   ```

## 备用机与 MQTT 独立（192.168.2.11）

备用机 .11 与主力机（.17）配置完全一致，且 **MQTT broker（docker 容器 my-mqtt）
各自本机独立运行**，互不依赖：两台的 `data/config/presentation-link.json` 中
mqtt.url 均为 `ws://127.0.0.1:8083/mqtt`。
⚠ 首次开机 DHCP 可能改 IP——mqtt.url 一律写 `127.0.0.1`，不要写局域网 IP
（.17 曾因 IP 从 .13 变为 .17 导致 LCD 推送静默失效）。

切换到备用机的步骤：
1. 屏幕接 .11，开机（GNOME 自动登录 ztl）；
2. `bash start-dual-kiosk.sh check` 自检全 ✓；
3. `bash start-dual-kiosk.sh`（或 restart）——两屏自动恢复上次放映画面；
4. 工牌/LED 大屏端的 broker 地址改为 `.11:8083`（broker 端口 1883/8083/8084）；
5. 需要同步主力机最新数据时（在主力机 .17 上执行，然后到 .11 重启 pptist）：
   `cd /home/ztl/ppt && sudo tar czf - pptist-rk3588-lcd-deploy/data | ssh ztl@192.168.2.11 'sudo tar xzf - -C /home/ztl/ppt/pptist-rk3588-lcd-deploy --strip-components=2 pptist-rk3588-lcd-deploy/data'`

.17 上其余 docker 服务（blockchains 区块链全家桶、lof-screen、rustdesk 远程）
也已完整迁移至 .11，可随时 `docker compose up -d` 启动，默认与主力机并行运行、
互不影响。

### 故障接管（备用机顶上 .13）

主力机（.13）宕机时，在备用机 .11 上执行：

```bash
bash /home/ztl/ppt/pptist-rk3588-lcd-deploy/pptist-failover.sh takeover
```

- 自动确认主力已失联（探测 8686/22/8083 服务端口），存活会拒绝（`--force` 强制）；
- .11 顶上 `.13` 并主动 GARP 广播，**工牌等设备无感切换**；
- 页面服务、broker 均为本机运行，接管后立即对外服务。

主力修复后回到主力机的操作：

```bash
# 1) 先在备用机 .11 上还原网络：
bash /home/ztl/ppt/pptist-rk3588-lcd-deploy/pptist-failover.sh restore
# 2) 重启主力机——开机守卫（pptist-ip-bootguard）会自动检测 .13 是否仍被
#    备用机占用，被占用时主力机自动退回 DHCP，避免 IP 冲突
# 3) 需要把主力机最新数据带回备用机：在备用机上
bash /home/ztl/ppt/pptist-rk3588-lcd-deploy/pptist-failover.sh sync-data
```

## 详细功能文档

随仓库附带：`doc/remote-default-ppt.md`（部署细节、协议、验证）、`doc/presentation-link.md`（MQTT/WebSocket 放映事件协议）。本包不含 MQTT/WebSocket 配置项改动方式：打开播放页右上角「放映联动」或在编辑器「≡ → 放映联动」中设置，配置保存在浏览器内。
