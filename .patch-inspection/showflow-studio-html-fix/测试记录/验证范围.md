# 本次验证范围

## 已执行

1. `html-service-tests.log`：31 组真实 Node 服务方法断言，使用临时目录，不使用生产数据。覆盖上传、校验、草稿/正式隔离、发布、下载字节一致、历史保护、重启恢复、切回 CSS、排队发布和最终 16 页清单。
2. `browser-offline-tests.log`：浏览器实际执行新宿主/桥接源码和最终 HTML。16 页逐页 NAVIGATE、真实 DOM 渲染后 ACK；未知 ID 不误 ACK；幂等；模拟重连 SYNC_STATE；双岗高亮；iframe 对父页面/存储的隔离；四种桌面尺寸。没有 JavaScript 页面异常。
3. 浏览器组件环境中的原 monitor JPEG 截图生成与 payload 构造（接收端为测试替身）。
4. `syntax-check.log`：6 个 JavaScript 文件通过 Node 语法检查；2 个 Vue script 块与 2 个 TS 文件通过 TypeScript 转译语法检查。这不是完整类型检查或 Vue 模板编译。
5. `installer-tests.log`：干净基准上的检查/应用/重复应用/源码回滚/版本不一致保护；data、dist、依赖、配置均未触碰。
6. `核心原文件未变更校验.json`：逐字节核对关键协议/Controller/LCD/依赖配置未改。

## 未完成，必须由老师环境验收

npm 包下载不可用，完整 `npm ci` 未完成；因此未完成 Vue 模板编译、全量 vue-tsc、生产 build、完整原回归套件。两个原 TypeScript 回归入口因缺少 nanoid 依赖退出，原始日志保留，不能计为通过。

浏览器本地 HTTP 访问被管理策略阻止，未绕过该限制。浏览器测试使用 about:blank 离线组件环境，fetch/WebSocket 为测试替身；服务方法测试是实际 Node 方法。两者不能合称实网端到端测试。

实际服务器浏览器、真实 `/showflow` WebSocket 网络路由、两块物理屏、PPT/LCD/MQTT 硬件联动、生产构建及部署回归均待老师在原环境验证。

包内截图是上述离线组件实际渲染，不是新生成的效果图，也不是服务器已部署截图。
